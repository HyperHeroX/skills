# GN1250301E 將無障礙名稱與可見標籤匹配

- **稽核評量碼**：GN1250301E
- **對應成功準則**：2.5.3
- **對應認證等級**：A
- **對應國際技術碼**：G211
- **類別**：General
- **訊息**：將無障礙名稱與可見標籤匹配
- **英文訊息**：G211: Matching the accessible name to the visible label

## 規則說明
語音識別軟體處理語音輸入並查找匹配項時，將使用控制元件的無障礙名稱。如果標籤中的文字與無障礙名稱中的文字不匹配，則可能對使用者造成問題。

## 檢測說明
程序

- 對於輸入控制元件，檢查每個輸入具有相鄰文字作為其標籤。
- 對於每個輸入，根據無障礙名稱計算，檢查整個文字字符串(不考慮字母大小寫和標點符號)是否與輸入的無障礙名稱匹配。
- 對於按鈕、鏈結、選單和其他非輸入控件，檢查每個包含文字作為其標籤的控制元件。
- 對於每個非輸入控制元件，檢查整個文字字符串(不考慮字母大小寫和標點符號)是否與輸入的無障礙名稱匹配。

預期結果：檢查#2和#4為是。

## 說明
參考資源：
- Accessible Name and Description Computation (https://www.w3.org/TR/accname-1.1/)
- Accessible Name and Description Computation in HTML Accessibility API Mappings 1.0 (https://www.w3.org/TR/html-aam-1.0/#accessible-name-and-description-computation)

相關技術：H44: Using label elements to associate text labels with form controls / H71: Providing a description for groups of form controls using fieldset and legend elements / H85: Using OPTGROUP to group OPTION elements inside a SELECT / ARIA17: Using grouping roles to identify related form controls
