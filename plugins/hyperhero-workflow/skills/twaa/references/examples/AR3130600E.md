# AR3130600E 使用ARIA地標來識別網頁的區域

- **稽核評量碼**：AR3130600E
- **對應成功準則**：1.3.6
- **對應認證等級**：AAA
- **對應國際技術碼**：ARIA11
- **類別**：ARIA
- **訊息**：使用ARIA地標來識別網頁的區域
- **英文訊息**：ARIA11: Using ARIA landmarks to identify regions of a page

## 規則說明

使用地標角色(landmark roles或landmarks)以程式化方式標識頁面的各個部分。

角色值如下：
- **banner（橫幅）**：包含頁面的主要標題或內部標題的區域。
- **complementary（補充）**：網頁中支持主要內容的任何部分，但其本身是獨立且具意義。
- **contentinfo（內容資訊）**：包含有關父網頁的訊息(例如版權和指向隱私聲明的鏈結)的區域。
- **form（表單）**：網頁的一個區域，代表與表單相關的元素的集合，其中一些元素可以表示可編輯的值，這些值可以提交給服務器進行處理。
- **main（主要內容）**：網頁中的主要內容。幾乎所有情況下，頁面將只有一個role="main"。
- **navigation（導覽）**：適用於在瀏覽網頁或相關網頁時使用的鏈結的集合。
- **search（搜尋）**：網頁的搜尋工具。
- **application（應用程式）**：聲明為Web應用程序的區域，而不是網頁。(注意：應謹慎使用應用程式的角色，因為它會向螢幕報讀軟體發出信號，以關閉正常的Web導覽控制元件。)

## 檢測說明

**程序**

1. 檢查每個具有landmark role地標角色的元素。
2. 檢查是否將地標角色屬性應用於與該角色對應的頁面部分。(即，"導航(navigation)"角色應用於導航部分，"主要內容(main)"角色應用於主要內容的位置。)

**預期結果**

#1和#2為是。

## 說明

參考資源：
- WAI-ARIA Authoring Practices 1.1 (https://www.w3.org/TR/wai-aria-practices/)
- Accessible Rich Internet Applications (WAI-ARIA) 1.0, Using WAI-ARIA Roles (https://www.w3.org/TR/wai-aria/#usage_intro)
- Accessible Rich Internet Applications (WAI-ARIA) 1.0, Supported States and Properties (https://www.w3.org/TR/wai-aria/#states_and_properties)
